from setuptools import setup, find_packages

setup(
    name = 'GenAI Project',
    version = '0.0.0',
    author= 'merriam',
    packages= find_packages(), # finds init as local packages
    install_requires=[]
)